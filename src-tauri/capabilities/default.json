{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "Only the local main window; no frontend shell, filesystem or HTTP permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
